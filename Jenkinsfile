pipeline {
    agent any

    stages {
        stage('first') {
            steps {
                echo "stage one for ${sohail}"
            }
        }
    }
    post {
        always {
            echo "this is ${pathan}"
        }
    }
}
